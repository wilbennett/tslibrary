{
  "name": "library",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "colors": "npm run colors:live",
    "test": "jest --notify",
    "continuoustest": "jest --watch --notify",
    "continuouscoverage": "jest --coverage --watch --notify",
    "colors:live": "nodemon --watch src/**/*.ts --exec ts-node src/zz/temp-color-test/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.25",
    "@types/node": "^12.12.22",
    "jest": "^24.9.0",
    "nodemon": "^1.19.4",
    "prettier": "1.18.2",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.4",
    "typescript": "^3.7.4"
  }
}
